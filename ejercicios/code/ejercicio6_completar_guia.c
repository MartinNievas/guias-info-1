#include <stdio.h>

/** Completar desde aquí */

/** Hasta aquí */

int main(void)
{
  int base, exp;

  printf("Ingrese base y explonente: "); scanf("%d %d", &base, &exp);
  printf("El número %d elevado a la %d es: %d\n", base, exp, potecia(base,exp));
  return 0;
}
